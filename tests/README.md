# Tests — CORE-HOST

Per-domain suites mirror `core/` plus `integration/`.
Run `python3 -m pytest tests/version/test_version.py -q` for smoke, full `python3 -m pytest -q`.
See `../README.md` and `../docs/lan-readiness.md` (LAN NOT PERFORMED).
